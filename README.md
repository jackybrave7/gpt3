# gpt3

This repository now contains a simple web-based calculator written in HTML, CSS, and JavaScript.

To use the calculator, open `index.html` in a web browser. Enter a mathematical expression (such as `2+2*5`) and click **Calculate** to see the result.
